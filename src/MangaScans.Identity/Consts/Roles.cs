namespace MangaScans.Identity.Consts;

public class Roles
{
    public const string Administrator = nameof(Administrator);
    public const string User = nameof(User);
    public const string Refresh = nameof(Refresh);
}